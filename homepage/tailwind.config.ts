import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./src/**/*.tsx"],
  darkMode: "media",
  theme: {
    // fontSize: {
    //   base: "12px",
    // },
  },
  plugins: [],
};
export default config;
